This directory contains shared functionality across the desktop application that is not a UI component. e.g. date utilities, string formatters, etc.

Each utility should be a pure function with unit tests.
