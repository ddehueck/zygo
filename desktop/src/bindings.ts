
// This file was generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta). Do not edit this file manually.

/** user-defined commands **/


export const commands = {
async listWorkflows(params: ListWorkflowsParams) : Promise<Result<Workflow[], string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_workflows", { params }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async listRuns(params: ListRunsParams) : Promise<Result<Run[], string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_runs", { params }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async listRunEvents(params: ListRunEventsParams) : Promise<Result<Event[], string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("list_run_events", { params }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async getWorkflowVersionSchema(params: GetWorkflowVersionSchemaParams) : Promise<Result<WorkflowVersionSchema, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("get_workflow_version_schema", { params }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async livestream(params: LivestreamParams) : Promise<Result<LivestreamResponse, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("livestream", { params }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
}
}

/** user-defined events **/



/** user-defined constants **/



/** user-defined types **/

export type Channel = { id: string; workflow_version_id: string; name: string }
export type Edge = { workflow_version_id: string; job_id: string; channel_id: string; kind: string }
export type Event = { id: string; workflow_version_id: string; workflow_run_id: string; is_replay: boolean; sequence_number: bigint | null; timestamp: string | null; event_kind: string; job_id: string | null; job_run_id: string | null; channel_id: string | null }
export type GetWorkflowVersionSchemaParams = { workflow_version_id: string }
export type Job = { id: string; workflow_version_id: string; name: string; content_hash: string }
export type ListRunEventsParams = { run_id: string; sequence_number: bigint | null; sort: SortOrder | null; limit: number | null }
export type ListRunsParams = { workflow_id: string; run_id: string | null; sort: SortOrder | null; limit: number | null }
export type ListWorkflowsParams = { workflow_id: string | null; sort: SortOrder | null; limit: number | null }
export type LivestreamCursor = { workflows: LivestreamWorkflowCursor | null; runs: LivestreamRunCursor | null; events: LivestreamEventCursor | null }
export type LivestreamEventCursor = { event_id: string | null; sequence_number: bigint | null; limit: number }
export type LivestreamParams = { cursor: LivestreamCursor | null }
export type LivestreamResponse = { workflows: Workflow[]; runs: Run[]; events: Event[]; next_cursor: LivestreamCursor | null }
export type LivestreamRunCursor = { run_id: string | null; limit: number }
export type LivestreamWorkflowCursor = { workflow_id: string | null; limit: number }
export type Run = { id: string; workflow_version_id: string; created_at: string | null }
export type SortOrder = "Unspecified" | "Asc" | "Desc"
export type Workflow = { id: string; name: string }
export type WorkflowVersionSchema = { jobs: Job[]; channels: Channel[]; edges: Edge[] }

/** tauri-specta globals **/

import {
	invoke as TAURI_INVOKE,
	Channel as TAURI_CHANNEL,
} from "@tauri-apps/api/core";
import * as TAURI_API_EVENT from "@tauri-apps/api/event";
import { type WebviewWindow as __WebviewWindow__ } from "@tauri-apps/api/webviewWindow";

type __EventObj__<T> = {
	listen: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.listen<T>>;
	once: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.once<T>>;
	emit: null extends T
		? (payload?: T) => ReturnType<typeof TAURI_API_EVENT.emit>
		: (payload: T) => ReturnType<typeof TAURI_API_EVENT.emit>;
};

export type Result<T, E> =
	| { status: "ok"; data: T }
	| { status: "error"; error: E };

function __makeEvents__<T extends Record<string, any>>(
	mappings: Record<keyof T, string>,
) {
	return new Proxy(
		{} as unknown as {
			[K in keyof T]: __EventObj__<T[K]> & {
				(handle: __WebviewWindow__): __EventObj__<T[K]>;
			};
		},
		{
			get: (_, event) => {
				const name = mappings[event as keyof T];

				return new Proxy((() => {}) as any, {
					apply: (_, __, [window]: [__WebviewWindow__]) => ({
						listen: (arg: any) => window.listen(name, arg),
						once: (arg: any) => window.once(name, arg),
						emit: (arg: any) => window.emit(name, arg),
					}),
					get: (_, command: keyof __EventObj__<any>) => {
						switch (command) {
							case "listen":
								return (arg: any) => TAURI_API_EVENT.listen(name, arg);
							case "once":
								return (arg: any) => TAURI_API_EVENT.once(name, arg);
							case "emit":
								return (arg: any) => TAURI_API_EVENT.emit(name, arg);
						}
					},
				});
			},
		},
	);
}
